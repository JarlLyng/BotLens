{
  "name": "botlens",
  "version": "1.0.0",
  "description": "Analyze if a website is readable by AI",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "node --test test/*.test.js",
    "build": "bash scripts/build.sh",
    "check:faq": "node scripts/check-faq-jsonld.js",
    "fix:faq": "node scripts/check-faq-jsonld.js --write"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  }
}
